# 2026-05-13 工作日志

## 1. 钉钉 MCP + 钉钉文档 MCP 配置

- 在 Claude Code 中配置官方钉钉 MCP (`dingtalk-mcp`) 和钉钉文档 MCP (`alidocs-mcp`)
- 通过 OpenClaw 的 DingTalk Stream connector 实现消息收发

## 2. 钉钉消息接收链路搭建

- **链路**: 钉钉 → OpenClaw dingtalk-connector → claude-notify hook → inbox 目录 → Claude Code 监控
- 创建 `~/.openclaw/hooks/claude-notify/handler.ts` 钩子
- 过滤 `message:received` 事件，将消息 JSON 写入 `~/.claude/projects/.../tmp/inbox/`
- 建立群聊和私聊消息的区分机制（群聊用 `cid:xxx` 格式，私聊用用户 ID）

## 3. 钉钉回复机制建立

- 使用 `openclaw message send --channel dingtalk-connector -t "target" --account "sales"` CLI 命令发送回复
- 群聊回复需使用 `group:cidxxx` 前缀，私聊直接使用用户 ID
- 回复前缀加 `[claude agent]` 标识来源
- 通过 `call_openclaw` MCP 调用 `sendToGroup`/`sendToUser` 网关方法

## 4. 钉钉 Markdown 渲染修复 ✅

**问题**: `sendTextToDingTalk` 硬编码 `msgType: "text"`，导致 markdown 不渲染

**修复**:
- `src/services/messaging.ts` — `sendTextToDingTalk` 中添加 markdown 自动检测：
  ```typescript
  const hasMarkdown = /^[#*>-]|[*_`#\[\]]/.test(text) || (text && typeof text === "string" && text.includes("\n"));
  ```
- 同修复应用到 `sendMediaToDingTalk` 函数
- 构建后 markdown 消息正常渲染

## 5. dingtalk-connector 升级 v0.8.6 → v0.8.19 ✅

- 从 GitHub upstream/v0.8.19 分支拉取新版本
- 适配新构建系统：从 Rollup 迁移到 tsdown
- 适配严格 config schema：
  - 移除无效属性：`gatewayBaseUrl`, `gatewayToken`, `sessionTimeout`
  - 将 `defaultAccount` 从 `accounts` 对象内移到顶层
  - 清理 JSON 配置中的 Python 调试输出污染
- 保留 markdown 渲染修复（messaging.ts 改动在 src 层，构建后仍生效）

## 6. Playwright MCP 安装 ✅

- 在 `~/.claude/settings.json` 中添加 `playwright-mcp` 配置
- 使用 `npx -y @playwright/mcp@0.0.75` 运行

## 7. 市场调研（mini printer / 热敏打印机）

- 通过网络搜索获取 mini printer 产品信息
- 调研 Temu 和 1688 平台热敏打印机销售数据：
  - 销售总量及价格分布（50 台以下占比）
  - 热销款式分析
- 记录市场分析群（conversationId: cidsDqAEsQtMKm0B1l/vseJ7w==）

## 8. OpenClaw AI 自动回复开关（已恢复默认）

- 在 `message-handler.ts` 中添加 `autoReply` 控制逻辑，默认关闭 AI 自动回复
- 后经用户确认，恢复 OpenClaw 默认行为（OpenClaw 和 Claude 都可以回复）

## 9. hyperframes 安装

- 从 GitHub HTTPS 克隆 `heygen-com/hyperframes` 仓库（17.7k stars）
- 使用 `--depth 1` 浅克隆解决 RPC 失败问题
- 安装 bun 包管理器后执行 `bun install`
